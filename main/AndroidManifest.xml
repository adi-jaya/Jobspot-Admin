<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jobspot.jobspot_admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParentActivity" >

        <activity
            android:name=".PerusahaanDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.ChildActivity"/>

        <!-- Perusahaan Belum Terverifikasi Activity -->
        <activity
            android:name=".PerusahaanBelumTerverifikasiActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:label="Perusahaan Belum Terverifikasi"
            android:theme="@style/Theme.ChildActivity" />

        <!-- Perusahaan Diblokir Activity -->
        <activity
            android:name=".PerusahaanDiblokirActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:label="Perusahaan Diblokir"
            android:theme="@style/Theme.ChildActivity" />

        <!-- Perusahaan Terverifikasi Activity -->
        <activity
            android:name=".PerusahaanTerverifikasiActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:label="Perusahaan Terverifikasi"
            android:theme="@style/Theme.ChildActivity" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" />

        <!-- Splash Screen Activity -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>